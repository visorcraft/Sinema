[0m
> build · k2p6
[0m
[0m→ [0mRead _review_diff.txt
[0m→ [0mRead app/src/main/java/com/sinema/model/ServerProfile.kt
[0m→ [0mRead app/src/main/java/com/sinema/util/ProfileCodec.kt
[0m→ [0mRead app/src/main/java/com/sinema/util/Prefs.kt
[0m→ [0mRead app/src/main/java/com/sinema/SinemaApp.kt
[0m→ [0mRead app/src/test/java/com/sinema/util/ServerProfileTest.kt
**CRITICAL**

- **`ProfileCodec.kt:13-18`** — `fromJson` catches broad `Exception` and returns `emptyList()`, causing **silent total data loss** on any JSON corruption or schema mismatch. Because Gson + Kotlin data classes throw on missing/null fields for non-nullable `String` properties, a future app update that adds a field to `ServerProfile` will wipe every user's profile list on first launch.

**MEDIUM**

- **`SinemaApp.kt:43`** — Cookie-refresh does a non-atomic read-modify-write (`prefs.profiles = prefs.profiles.map { … }`). If a profile switch or another refresh races between the read and write, one update will be lost.
- **`Prefs.kt:58-59`** — `activeProfile` falls back to `profiles.firstOrNull()` when `activeProfileId` is empty or points to a deleted profile. The cookie-refresh callback then updates the first profile, which may not be the one the user considers active.
- **`Prefs.kt:62-76`** — `migrateToProfilesIfNeeded()` never clears legacy fields after migration. The app continues to read/write both legacy accessors and the profile list, creating two sources of truth that can diverge if anything writes to the legacy getters/setters directly.

**LOW / STYLE**

- **`Prefs.kt:65`** — Uses fully-qualified `java.util.UUID.randomUUID().toString()`; prefer an import.
- **`ServerProfileTest.kt:7-18`** — Tests only cover a single-profile happy path and garbage input. Missing coverage for: multi-profile lists, empty list, missing/null fields, and active-profile behavior.

**VERDICT approved issues=5**
